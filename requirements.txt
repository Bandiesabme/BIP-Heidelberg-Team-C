# Core
opencv-python-headless  # OpenCV without GUI (lighter for Pi)
numpy                   # Array operations

# Sign Detection (choose one)
ncnn                    # NCNN python bindings for YOLO inference (lighter than ultralytics)
# OR
# tflite-runtime        # If using TFLite instead of NCNN

# Voice Commands
vosk                    # Offline speech recognition
sounddevice             # Microphone input

# Utilities
Pillow                  # Image handling

# NOTE: 'picarx' is pre-installed on the SunFounder Pi image.
# Install it separately if needed: pip install picarx
